{
  "name": "webcam-faceauth",
  "version": "1.0.0",
  "private": true,
  "description": "Facial recognition authentication web app",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node server.js"
  }
}
